<Window x:Class="Life.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Life"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Window.Resources>
        <ControlTemplate TargetType="CheckBox" x:Key="ChBoxTemplate">
            <CheckBox>
            </CheckBox>
        </ControlTemplate>
    </Window.Resources>
    <Grid>
        <ScrollViewer Height="500" Width="700">
            <Canvas Height="700" Width="700">
            <Ellipse x:Name="elipse1" Height="100" Width="100" StrokeThickness="2" Stroke="Black" MouseLeftButtonDown="elipse1_MouseLeftButtonDown">
                
            </Ellipse>
                
        </Canvas>
            
        </ScrollViewer>

    </Grid>
</Window>
